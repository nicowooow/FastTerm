import { VerticalBox } from "std-widgets.slint";
export component HistoryCommand {
    vertical-stretch: 1;
    VerticalBox {
        Text {
            text: "Command's history";
            font-size: 16px;
        }
    }
}
