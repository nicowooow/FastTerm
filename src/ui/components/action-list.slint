import { VerticalBox, GroupBox } from "std-widgets.slint";
export component ActionList {
    VerticalBox {
        GroupBox { }

        Text {
            text: "First Action";
        }

        Text {
            text: "Second Action";
        }

        Text {
            text: "Third Action";
        }

        Text {
            text: "Fourth Action";
        }
    }
}
