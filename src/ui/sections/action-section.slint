import { VerticalBox, GroupBox } from "std-widgets.slint";
import { Action } from "../components/action.slint";
export component ActionSection {
    VerticalBox {
        GroupBox { }

        Action { }
    }
}
